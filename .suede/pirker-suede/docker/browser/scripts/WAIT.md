# `wait.js` - Wait for Conditions

```bash
./scripts/wait.js 'div.loaded'              # wait for selector (30s default)
./scripts/wait.js 'div.loaded' --timeout 10
./scripts/wait.js --idle                    # wait for network idle
./scripts/wait.js --load                    # wait for page load event
```
