# `watch.js` - Background Logger

```bash
./scripts/watch.js                          # log console, errors, network -> JSONL
./scripts/watch.js --target <id>
```

Writes JSONL to `$LOG_DIR/<date>/<targetId>.jsonl`.
