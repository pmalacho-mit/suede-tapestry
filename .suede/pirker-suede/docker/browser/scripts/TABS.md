# `tabs.js` - Manage Tabs

```bash
./scripts/tabs.js                           # list all open tabs
./scripts/tabs.js --close <target-id>       # close specific tab
./scripts/tabs.js --close-all               # close all but first
```
