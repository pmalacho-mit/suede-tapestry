# `type.js` - Type Into Inputs

```bash
./scripts/type.js 'input[name="email"]' 'user@example.com'
./scripts/type.js '#search' 'hello' --clear --enter
```

- `--clear` empties the field first.
- `--enter` presses Enter after typing.
